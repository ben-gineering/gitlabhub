# Forgejo instance URL (with port if non-standard)
FORGEJO_URL=http://10.20.2.3:3000

# Forgejo API token
# Create at: Settings → Applications → Generate New Token
# Scopes needed: write:repository, read:user
FORGEJO_TOKEN=
